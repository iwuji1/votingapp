<%= form_with(model: option, local: true) do |form| %>
  <% if option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :votes %>
    <%= form.number_field :votes, id: :option_votes %>
  </div>

  <div class="field">
    <%= form.label :candidate_id %>
    <%= form.number_field :candidate_id, id: :option_candidate_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
